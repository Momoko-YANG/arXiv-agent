# arXiv Research Agent v2.0

# 核心
schedule>=1.1.0          # 定时任务调度
requests>=2.28.0         # HTTP（Telegram / S2 / Crossref API）

# 智能 Agent
openai>=1.0.0            # OpenAI API 客户端
httpx>=0.25.0            # HTTP 客户端（OpenAI SDK 超时 + 重试配置）

# 可选
PyYAML>=6.0              # YAML 配置文件支持
python-dateutil>=2.8.2   # 日期处理

# 测试
pytest>=7.0.0            # 测试框架
